# AGENTS.md

## Project

Simple single-file Snake game in Python using `pygame`.

## Running

```bash
pip install pygame
python snake_game.py
```

## Notes

- No tests, linting, or type checking.
- Single script with a procedural game loop; no package structure.
- `GEMINI.md` exists but contains generic context — verify commands against `README.md` or the script itself.